from dangerous_ui import DangerousUI
from dangerous_writer import Timer

# UI -------------------------------------------------------------------------------------------------------------------

if __name__ == "__main__":
    # Create dangerous_writer timer
    #timer = Timer()
    # Create window
    window = DangerousUI(10)
    window.mainloop()
